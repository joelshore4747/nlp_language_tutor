Turn the n-gram model into a trigram, it current is a bigram which sees two words and registers if they are common
when close together and a trigram will require more data but it will use three words instead of two meaning
more accuracy 1 